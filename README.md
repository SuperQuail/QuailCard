# QuailCard

基于 Rust、Tauri 2、Vue 3 和 TypeScript 的本地 Markdown 笔记与记忆卡片桌面应用。

```powershell
pnpm install
pnpm tauri dev
```

`pnpm dev` 可在浏览器中以内存演示数据运行界面（无需编译 Rust）。

## 构建

```powershell
pnpm tauri build
```
